apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ prometheus_server | default('prometheus') }}:{{ prometheus_port | default('9090') }}
    isDefault: true
    editable: false
    version: 1 
